server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jobservice_db
    username: root
    password: test

  jpa:
    hibernate:
      ddl-auto: update

  application:
    name: JOB-SERVICE

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
