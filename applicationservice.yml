server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/applicationservice_db
    username: root
    password: test

  jpa:
    hibernate:
      ddl-auto: update

  application:
    name: APPLICATION-SERVICE

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
